===============================================INTRO AND LOCAL SETUP=================================================
as we move into production, we realize managing a kubernetes cluster is not an easy task
we need a framework which sits on top of kubernetes and with minimal effort, lets us to manage the cluster

there can be lot of yamls we need to interact with while using kubernetes. HELM chart provides a very high abstraction with which u can run a minimal command
which internally communicates with kubernetes cluster producing same results but more effeciently

- using Kubernetes alone: You manually create, maintain, and version multiple .yaml files. Even small changes mean editing multiple places and applying each file with kubectl.
- Helm: You wrap all those YAMLs into a chart (i.e. a reusable, version-controlled package), and control everything with simple Helm commands.

let us suppose we have 3 yaml files normally which we write in kubernetes(let their values be as is hardcoded...as we move forward we replace them with references):

manually run kubectl commands:
kubectl apply -f deployment.yaml
kubectl apply -f service.yaml
kubectl apply -f ingress.yaml

helm replacement of above commands(deployment of container):
helm install mp-app ./my-chart

manually run kubectl commands:
kubectl delete -f deployment.yaml
kubectl delete -f service.yaml
kubectl delete -f ingress.yaml

helm replacement of above commands(deletion of container):
helm uninstall mp-app


📦 Helm Chart Structure (Simplified)
When you run:
helm create my-chart
You get:
my-chart/
├── Chart.yaml             # Metadata
├── values.yaml            # Config values
└── templates/
    ├── deployment.yaml    # Parameterized
    ├── service.yaml
    ├── ingress.yaml
    └── _helpers.tpl       # Label/name functions

install microk8s for a kubernetes cluster which u need as a prerequisite for experimenting with helm or if u have kind or minikube...u r good to go

Method1=>
🎉 Helm works perfectly fine with Kind (Kubernetes IN Docker)
✅ Helm Compatibility with Kind
- Kind creates a local Kubernetes cluster using Docker containers.
- Helm interacts with the Kubernetes API server using the same kubeconfig that kubectl uses

Method=>2
If you ever switch to MicroK8s later, you can enable Helm with:
microk8s enable helm
then install kubectl and configure .kubeconfig
1. Install MicroK8s
On Ubuntu/Debian:
sudo snap install microk8s --classic

2. Add Your User to MicroK8s Group
sudo usermod -a -G microk8s $USER
sudo chown -f -R $USER ~/.kube

Then log out and back in, or run:
newgrp microk8s

3. Enable Helm in MicroK8s
microk8s enable helm

Optionally, enable other add-ons:
microk8s enable dns storage ingress dashboard

4. Configure kubeconfig Access (Optional but handy)
If you want to use regular kubectl and Helm instead of microk8s kubectl:
mkdir -p $HOME/.kubeconfig
cd $HOME/.kubeconfig
microk8s config > config
export KUBECONFIG=$HOME/.kubeconfig/config

Now your regular kubectl and helm commands work seamlessly with MicroK8s.




once cluster is setup:
kubectl get all --all-namespaces

install the helm chart:
curl -L https://git.io/get_helm.sh | bash -s -- --version v3.8.2
ls -later
helm version


===========================================HELM CHART ARCHITECTURE==========================================
Helm is often described as the “package manager for Kubernetes.” It lets you define, install, and upgrade even the most complex Kubernetes apps using reusable templates called charts.


1. Helm CLI (helm)
- The command-line tool you use to interact with Helm.
- Fetches charts from a repository, installs them, upgrades them, and much more.
2. Chart Repository
- Like npm for Node or pip for Python: a place where Helm charts are stored and versioned.
- You can use public repos (e.g., Bitnami) or host your own.
3. Helm Charts
- Bundles of YAML files: parameterized templates for Kubernetes resources (Deployments, Services, Ingress, etc.).
- Located inside the templates/ folder, configured using values.yaml

4. kubectl / kubeconfig
- Helm uses kubectl under the hood to interact with your Kubernetes cluster.
5. Kube API Server
- The gateway to your Kubernetes cluster for helm to have communication with your cluster
- Helm sends chart manifests via this API, where Kubernetes processes and applies them.


🔁 What Happens When You Run helm install mychart?
- Chart Fetch: Helm gets the chart from a repo or local folder.
- Template Render: It merges values from values.yaml with the templates.
- Manifest Creation: YAML manifests for Kubernetes resources are rendered.
- Kube API Push: Manifests are sent to Kubernetes through the kube API.
- App Deployment: Kubernetes takes over, scheduling pods and spinning up services.

basic(commands):
helm install myhelloworld helloworld
helm delete myhelloworld
helm list -a
helm uninstall helloworld

========================================================A DEEPER DIVE: Basic hello world helm chart=========================================================================
let suppose we have a cluster
let there are one master and 2 worker nodes
kubectl get nodes               =>lists control-plane and 2 worker nodes

goto official documentation(helm.sh/docs) of helm and chose ur package manager to download helm-chart using commands
######curl and apt get commands

check installation:
helm version

helm create <chartname>             =>creates a directory with 2 files and 2 folders
ex: helm create helloworld

tree <chartname ex:helloworld>

cd <chartname>
ls -lrt
find .                      =>lists everything ..or u can run


we will try deploying this helloworld helm chart in our kubernetes cluster
vi values.yaml              =>change service type to NodePort

cd ../

now install helmchart using -helm install:
helm install myhelloworld helloworld
helm install <customname which u want to deploy in k8s> <directoryname_u_created where the created helm chart exists>
-we can call it simply:
helm install <release-name> <chart-directory>


verification:
helm list -a
kubectl get service
kubectl get all        # See pods, deployments, services — everything
kubectl describe service myhelloworld      # Deep dive into ports and selectors


find the assigned PORT=>
kubectl get service myhelloworld -o wide

Then find IP=>
-if u are using minikube:
minikube ip
-If you're on Kind (Kubernetes in Docker), use:
kubectl get nodes -o wide

test the service:
curl http://<node-ip>:<node-port>

NOTE: You can override NodePort like this while editing values.yaml:
service:
  type: NodePort
  port: 80
  nodePort: 31000  # Desired port


========================================================================K8S UI DASHBOARDS===========================================================================================
you can access the Kubernetes Dashboard using Kind or Minikube, just like your instructor does with MicroK8s. Each tool has its own quirks, but the dashboard setup is totally doable across all three.

with microk8s=>
microk8s dashboard-proxy                        =>copy the token 
you can open it on localhost:<portshown>        =>paste the token and click on sign in

🧱 With Kind (Kubernetes IN Docker):
Kind doesn’t include the dashboard by default, install it manually:
- Deploy the Dashboard:
kubectl apply -f https://raw.githubusercontent.com/kubernetes/dashboard/v2.7.0/aio/deploy/recommended.yaml
- Create Admin Access:
kubectl create serviceaccount admin-user -n kubernetes-dashboard
kubectl create clusterrolebinding admin-user \
  --clusterrole=cluster-admin \
  --serviceaccount=kubernetes-dashboard:admin-user
- Start Proxy:
kubectl proxy
- Access Dashboard: Open:
http://localhost:8001/api/v1/namespaces/kubernetes-dashboard/services/https:kubernetes-dashboard:/proxy/
- Login with Token:
kubectl -n kubernetes-dashboard describe secret \
$(kubectl -n kubernetes-dashboard get secret | grep admin-user | awk '{print $1}')


🖥️ With Minikube
Minikube makes it even easier:
- Start Minikube:
minikube start
- Launch Dashboard(This opens the dashboard in your browser automatically):
minikube dashboard
- Optional: Get just the URL
minikube dashboard --url

in cloud: u can replace these commands with their respective dashboard commands



🧠 Summary
| Tool | Dashboard Setup | Access Method | Notes | 
| MicroK8s | Built-in | microk8s dashboard-proxy | Your instructor’s setup | 
| Kind | Manual install | kubectl proxy | Needs token login | 
| Minikube | Built-in | minikube dashboard | Easiest for local dev | 

=============================================================HELM CLI COMMANDS===========================================================================================

- create a helm chart:
helm create <chartname>             => u can deep dive into this

- installation of helm chart
helm install <releasename> <chartname>

- list charts
helm list -a                        =>let this shows revision=1

- if u changed/want to make a new change into your helm chart(let us say u changed replicaCount in Values.yaml), 
u need to run upgrade command so that new changes will be applied to helm chart
helm upgrade <releasename> <chartname>                        =>u will witness a new revision, revision=2

- you can check revision also by:
helm list -a

- rollback to specified release i.e. to a revision:
helm rollback <releasename> <revisionnumber u want to rollback to>
this also is a revision and thus revision will be 3 by now eventhough state is switched to previous

- debug and dry run ur helm chart: validate helm chart before install/upgrade to know errors/misconfigurations
helm install <releasename> --dry-run --debug <chartname>      =>communicates with k8s API server(verify and validate helm chart against k8s)

- helm template:renders the chart templates locally(similar to previous ...but validates yamls without connecting to k8s API server)
helm template <chartname>

- Find errors or misconfigurations:
helm lint <chartname>

- uninstall helm chart:
helm uninstall <releasename>
helm list -a
==============================================================REPLACING HARDCODED VALUES: USE OF REFERENCES==============================================================================
inspect all files and folders=>content inside them using nano/vim/vi
upon inspection u will find that values are parameterized/templated or just reference is given...actual value is picked from somewhere else

🧪 The Helm Magic — Reference Over Hardcoding
Instead of hardcoding values like replicas, labels, ports, etc. inside deployment.yaml, Helm lets you:
- 🧩 Reference reusable values from one central place (values.yaml)
- 🧬 Inject metadata dynamically (Release, .Chart)
- 🛠️ Use helper templates for consistency (_helpers.tpl)
Result? You only touch values.yaml to customize anything — deployments, services, ingress, all adapt automatically via templating.

📁 How Values Are Mapped
| Reference Type    | Comes From                    | Example Usage                           | 
| .Values.*         | values.yaml                   | .Values.replicaCount                    | 
| .Chart.*          | Chart.yaml                    | .Chart.name, .Chart.version             | 
| .Release.*        | Helm's runtime metadata       | .Release.name, .Release.namespace       | 
| {{ include ... }} | _helpers.tpl helper functions | {{ include "sample-chart.fullname" . }} | 


Example: You want to deploy into a new namespace or change port number — just tweak values.yaml. No digging into templates.

“Any string like yourchartname.something(ex:sample-chart.fullname,sample-chart.labels etc ) is a reference to a helper function defined in _helpers.tpl. 
These helpers return values, often used for naming or labeling resources consistently.”


====================================================REAL WORLD USECASE: A DEMO====================================================
Let’s say you’re deploying hello-app to:
- Dev → 1 replica, port 8080
- Staging → 3 replicas, port 9090
- Prod → 5 replicas, port 80

🧵 The Challenge Without Helm
In a typical Kubernetes setup without Helm, you'd probably:
- Copy your deployment.yaml and other resource files into separate folders like /dev, /staging, /prod
- Manually tweak replicas, ports, labels, annotations, etc. for each environment
- Risk introducing inconsistency or human error while editing YAMLs
This quickly gets messy and hard to maintain, especially when you need to update all environments later 😬

🎯 Helm’s Approach — One Chart, Many Environments
With Helm:
- You create one chart with template files (deployment.yaml, service.yaml, etc.) inside the /templates folder
- Inside these templates, values are not hardcoded — instead, they’re referenced:
replicas: {{ .Values.replicaCount }}
ports:
  - containerPort: {{ .Values.service.port }}
- Then you create multiple values.yaml files — one per environment — that feed different values into the same chart

🛠️ How It Looks in Practice
Let’s say your Helm chart is in ./sample-chart.
Now, create three files:
📄 values-dev.yaml
replicaCount: 1
service:
  port: 8080

📄 values-staging.yaml
replicaCount: 3
service:
  port: 9090

📄 values-prod.yaml
replicaCount: 5
service:
  port: 80

Now deploy each environment like this:
helm install hello-dev ./sample-chart -f values-dev.yaml
helm install hello-staging ./sample-chart -f values-staging.yaml
helm install hello-prod ./sample-chart -f values-prod.yaml


Helm will:
- Render the same templates (deployment.yaml, etc.) with different values
- Automatically assign names (hello-dev, hello-prod…) from the release name
- Use values from the corresponding file without modifying any YAML template manually

📦 Why This Rocks
- ✅ You maintain a single source of truth — your chart
- 🔁 Easy to update logic (e.g. label changes, resource settings) across all environments
- 🧪 Environment-specific customization is clean and isolated
- 🚀 Super scalable as you add more environments or clients

example:sample-chart/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "sample-chart.fullname" . }}
  labels:
    app: {{ include "sample-chart.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    environment: {{ .Values.env }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "sample-chart.name" . }}
  template:
    metadata:
      annotations:
        {{- toYaml .Values.podAnnotations | nindent 8 }}
      labels:
        app: {{ include "sample-chart.name" . }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
          ports:
            - containerPort: {{ .Values.service.port }}

🔍 What’s Going On Here?
- .Values.replicaCount, .Values.service.port, .Values.image.* → from your values.yaml file depending on environment
- .Release.* → Automatically injected by Helm during install (like release name)
- .Chart.* → from Chart.yaml metadata
- {{ include "sample-chart.fullname" . }} → Uses helper from _helpers.tpl to consistently name resources

Let’s say your dev values.yaml has:
replicaCount: 1
env: dev
service:
  port: 8080
image:
  repository: myregistry/hello-app
  tag: "dev"

When you run:
helm install hello-dev ./sample-chart -f values-dev.yaml

It’ll generate a Deployment where:
- Name becomes something like hello-dev-sample-chart
- Replicas = 1
- Port = 8080
- Image = myregistry/hello-app:dev
- Labels/annotations match the dev context
we can do the same for prod using values-prod.yaml, and your chart adapts without touching the template.
================================================COMPARISION OF SOME ENVIRONMENTS=============================================
📄 Environment-Specific values.yaml Structures
| Key            | values-dev.yaml         | values-staging.yaml          | values-prod.yaml       | 
| replicaCount   | 1                       | 3                            | 5                      | 
| env            | dev                     | staging                      | prod                   | 
| service.port   | 8080                    | 9090                         | 80                     | 
| image.repository| "myregistry/hello-app" | "myregistry/hello-app"       | "myregistry/hello-app" | 
| image.tag      | "dev"                   | "staging"                    | "prod"                 | 
| podAnnotations | {} or test annotations  | staging-specific annotations | production annotations | 



🧬 What This Means
- All values are read from these files and injected into your deployment.yaml, service.yaml, etc. using Helm's templating engine.
- Your actual chart structure stays unchanged. You only swap out the config via the CLI:
helm install hello-dev ./sample-chart -f values-dev.yaml
helm install hello-staging ./sample-chart -f values-staging.yaml
helm install hello-prod ./sample-chart -f values-prod.yaml

Helm handles the rendering and creates Kubernetes manifests customized for each environment 

==================================HEML AS PACKAGE MANAGER=========================================
similar to windows: choco, mac: brew, ubuntu: apt, redhat: yum and all package managers

HELM: also as a package manager for kubernetes

why we need it?
let us say we need to install ARGOCD(let a ci/cd tool)
1.traditional way=>download binaries=>follow instructions
2.if we install it as pod kubernetes=>we need to manage seprate deployment, service, role/binding, policies, namespace
and all other yamls manually


SOLUTION: HELM=>bundles all these together whatever we specified above...deployment, service, policies etc => collectively called as HELM CHART
the chart is stored on helm REPOSITORY...artifacts.io   -> central repo for charts
when we need to install the package...we pull chart from repo and create a RELEASE(running instance) out of it

charts are reusable -> single chart into multiple environments
each time we deploy a chart -> a new release is created

let us suppose we need to install argocd:

- Search Helm charts on
artifacthub.io => search for the package > Intsall

copy commands and do as directed=>
- Add the Helm repo
helm repo add argo https://argoproj.github.io/argo-helm
helm repo update

- Install ArgoCD chart:
helm install my-argo-cd argo/argo-cd --version 7.8.10
--version is optional; omit it for the latest version.


📟 Access the ArgoCD UI (Post-install)...it will direct u how to use:
ex=>inorder to access the UI do following
1.kubectl port-forward service/my-argo-cd-server -n default 8080:443
- Open browser to https://localhost:8080
- Login using ArgoCD’s default credentials (you’ll find the admin password via kubectl get secret)

Check installed charts=>
helm list

🔄 Upgrade to latest version:
helm upgrade my-argo-cd argo/argo-cd                

rollback any changes(ex:rollback latest chart):
helm rollback <chartname ex:my-argo-cd>
helm list                                   =>shows revision history

search the helm hub(Search public Helm hub)/repo(repos you’ve added):
helm search hub <what u want to search ex: argo or argo-cd etc>
helm search repo <package/anything>

📦 Download Chart Locally (Optional Exploration)pull the package as a tar file:
helm pull argo/argo-cd           # downloads .tgz chart file
ls -lrt                          # verify download
tar -xvf argo-cd-*.tgz           # extract contents


- To uninstall a release:
helm uninstall my-argo-cd

- To see configurable options:
helm show values argo/argo-cd > reference.yaml

- Want to override defaults? Create a custom values.yaml, then:
helm install my-argo-cd argo/argo-cd -f values.yaml









