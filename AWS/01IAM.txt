Identity Access Manager/Management:

Users: can have permission policies attatched
1.IAM User
- Created under the AWS account.
- Can be assigned fine-grained permissions via IAM policies.
- Ideal for developers, admins, or applications needing access.
2.Root User
- Created when the AWS account is first set up.
- Has full access to all services and resources.

AWS Console access:
- Account ID or alias
- IAM username
- Password
These credentials can be downloaded as a .csv file during user creation.



Programmatic Access
- For CLI, SDKs, or API access, you need:
- Access Key ID
- Secret Access Key
- region
- These are generated during user setup:
Follow the prompts in the AWS Console.
Save them securely — you won’t be able to view the secret again.
Optionally download them as a .csv file.

============================================================
👤 IAM Users
• 	Represent individual identities (people or applications).
• 	Each user has:
• 	A username
• 	Optional console access (via password)
• 	Optional programmatic access (via access keys)
• 	Users can be assigned permissions directly or through groups.
🧠 Example: You create a user  who needs access to S3 and EC2.


👥 IAM Groups
• 	A collection of IAM users.
• 	Used to assign permissions collectively.
• 	Users can belong to multiple groups.
• 	Groups cannot be nested (no group inside a group).
🧠 Example: You create a group  with read/write access to S3. Add  to this group — she inherits those permissions.


📜 IAM Policies
• 	JSON documents that define permissions.
• 	Can be attached to:
• 	Users
• 	Groups
• 	Roles
• 	Two main types:
• 	Identity-based policies: Attached to users, groups, or roles.
• 	Resource-based policies: Attached directly to AWS resources (like S3 buckets).
🧠 Example Policy:
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": "s3:*",
      "Resource": "*"
    }
  ]
}
This grants full access to all S3 resources.


🎭 What Is an IAM Role?
An IAM role is:
- An identity with permissions, just like a user.
- Assumed temporarily by whoever needs it.
- Used to delegate access securely.
roles are not tied to a specific person. Instead, they can be assumed by trusted entities like AWS services, users, or even external identities.
Roles are perfect for situations where you want to:
- Let an EC2 instance access S3 without hardcoding credentials.
- Allow a Lambda function to read from DynamoDB.
- Grant cross-account access between AWS accounts.
- Enable federated users (e.g., from Google or Active Directory) to use AWS.
Trust Policy: Defines who can assume the role (e.g., EC2, another AWS account, SAML)
Permissions Policy: Specifies what actions the role can perform on which resources
Session Duration: Temporary credentials last for a set time (default: 1 hour)