VM is an entire operating system with its own kernel, hardware drivers, programs, and applications, creating which is an overhead
container is simply an isolated process with all of the files it needs to run.(Slef-contained, Isolated(file system), independent and portable)
A container image is a standardized package that includes all of the files, binaries, libraries, and configurations to run a container.(Immutable and layered)
Registry is a centralized location for storing and sharing your container images. It can be either public or private.

====================================================================================================================================================

Docker Engine as this is the core technology that powers Docker containers
Docker desktop: User-friendly GUI for management of containers...includes docker engine also

As devops engineer use commands instead of GUI
It's possible to run both Docker Desktop and Docker Engine simultaneously, 
but there may be situations where running both at the same time can cause issues.
So we will focus more on commands and not GUI

Stop Docker engine service:
>sudo systemctl stop docker docker.socket containerd
If docker automatically starts when machine starts, to prevent it:
>sudo systemctl disable docker docker.socket containerd

============================================================================================
Installation:

uninstall all conflicting packages(unofficial docker packages):
>sudo apt remove $(dpkg --get-selections docker.io docker-compose docker-compose-v2 docker-doc podman-docker containerd runc | cut -f1)

1.Setup docker's apt repo:
# Add Docker's official GPG key:
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

# Add the repository to Apt sources:
sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://download.docker.com/linux/ubuntu
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update

2.Install docker packages:
>sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

3.Start docker:
>sudo systemctl start docker

4.Verify running status:
>sudo systemctl status docker

5.Verify installation succes:
>sudo docker run hello-world

===========================================================================================
installation of docker on linux: easiest(only for learning not for production environments)
>sudo apt install docker.io              =>for ubuntu distro

check installation:
>docker --version

start docker daemon:
>sudo systemctl start docker         (or)
>dockerd &               =>if systemctl not available(-  systemctl commands won’t work in most containers)
🔧 Prerequisites
- Make sure containerd is installed and running.
- You may need to install iptables, cgroup support, and other dependencies depending on your base image.

================================================================================================================
Docker in docker:

🧠 Alternative(in place of installation): Use Host Docker from Inside Container
If your goal is just to run Docker commands from inside the container, you can mount the host’s Docker socket:
>docker run -v /var/run/docker.sock:/var/run/docker.sock -it ubuntu
This lets you use the host’s Docker daemon without needing to start one inside the container.

If your goal is to run Docker inside Docker, the recommended way is to use the official Docker-in-Docker image:
>docker run --privileged --name dind-test -d docker:dind

====================================================================================================================

Post installation(Not mandatory but recommended):
Create docker group and add your user:
>sudo groupadd docker
>sudo usermod -aG docker $USER
>newgrp docker                         :activate changes to groups

changing ownership and permissions:
>sudo chown "$USER":"$USER" /home/"$USER"/.docker -R
>sudo chmod g+rwx "$HOME/.docker" -R

=========================================================================================================================

Automatically start docker and containerd on boot:
>sudo systemctl enable docker.service
>sudo systemctl enable containerd.service

Stop this behaviour:
>sudo systemc
tl disable docker.service
>sudo systemctl disable containerd.service

=======================================================================================================================

download an image to local system:
docker pull <imagename>

docker run <imagename>

docker ps -a        => all containers
docker ps -aq       =>get all container ids

docker run -it <image>          =>run a container with the specified image interactively: i->interactive t->terminal attached
docker restart <containerid>        =>restart a stopped container
docker exec -it <containerid> <terminal:bash/sh>        =>use its terminal

docker start/restart <containerid>      =>start a stopped container
docker stop <containerid>               =>stop a container

docker rm $(docker ps -a)       =>remove all stopped containers
docker rmi imagename            =>remove an image built in local env



What Are Dangling Images in Docker?
Dangling images are Docker images that:
Have no name (tag) → their repository and tag are <none>:<none>
Are not used by any container like these:
REPOSITORY          TAG       IMAGE ID       CREATED         SIZE
<none>              <none>    abc12345defg   10 minutes ago  300MB
They are intermediate build layers or outdated images left behind

Remove only dangling images:
docker image prune

Remove all unused images (dangling + unreferenced(nolonger being used by containers)):
docker image prune -a



docker logs <containerid>       =>show logs of that container
Container logs:
Docker only captures logs from the main process.

Example:
CMD ["python", "app.py"]
✔ print() statements in python code appear in docker logs

But if app logs to file:
logging.basicConfig(filename="/var/log/app.log")
❌ docker logs shows nothing

let us say we have a container running in detached mode...we want to run a command...we dont want to open interactive terminal again, we use docker exec
docker exec <container_name> <command>

docker inspect <containerid>           =>gives info about the container imageused, mounts/vols, path, ips, ports, envvars, workdir etc etc

building a dockerimage from dockerfile
docker build  -t <imagename>:<optional tag ex:v1/v2..latest> <directory of dockerfile:use '.' if current and file is named as "Dockerfile">

portmapping=>gateway from local machine to the container
let us say we run an nginx container=>nginx usually runs on port 80...we pull and run it it will run no issues
but when we try accessing typing localhost:80 on our browser theres nothing in there
coz browser is on our machine which has a different 80 port which is not nginx containers 80port

so we map ourmachineport:nginxport  => 7999:80  =>we can now access nginx on port:7999 on our localhost
docker run -it -p 8000:80 nginx


RUN vs ENTRYPOINT vs CMD
RUN Purpose:
Used during the image build process.
Executes commands once when building the image (like installing packages or setting up files).
RUN apt update && apt install -y curl

CMD Purpose:
Provides default arguments or a command to run when the container starts.
Can be overridden when starting a container using docker run.
CMD ["python", "app.py"]
overridable while running=> docker run my-image python other_script.py

ENTRYPOINT Purpose:
Defines the main executable for the container.
Not easily overridden (unless using --entrypoint flag).
ENTRYPOINT ["python"]
CMD ["app.py"]
======================================================================================================================
Image layers:  images are composed of layers. And each of these layers, once created, are immutable and those layers used to create the filesystem a container can use.
A layer is: A snapshot of filesystem changes...layers live on the host machine not inside container.(something like: /var/lib/docker)
“What got added / removed / modified”

Example:
Ubuntu base → one layer
Install Node → another layer
Add app.js → another layer

Let’s look at a theoretical image:
The first layer adds basic commands and a package manager, such as apt.
The second layer installs a Python runtime and pip for dependency management.
The third layer copies in an application’s specific requirements.txt file.
The fourth layer installs that application’s specific dependencies.
The fifth layer copies in the actual source code of the application.

It allows layers to be reused between images. For example, imagine you wanted to create another Python application. Due to layering, you can leverage the same Python base
This will make builds faster and reduce the amount of storage and bandwidth required to distribute the images

=======================================================================================================================
With Docker Compose, you can define all of your containers and their configurations in a single YAML file.
docker compose:making services run with minimal setup and cleanup effort

localhost for one doesnt mean same localhost for other
Why localhost doesn't mean the same across Docker containers?
(incase u dockerized a frontend(5173) and backend(3000) and frontend needs to make api call, it can't simply call on localhost:8000 while it will work when u dont use docker to run the project)
if your frontend container tries http://localhost:3000, it’s looking for a backend process inside the same container, which doesn’t exist.
Each Docker container has its own isolated network namespace — meaning each one is seperate instance...each is a seperate machine:
localhost (or 127.0.0.1) inside a container refers to itself — not the host or any other container.
So if Container A tries to call localhost:5000, it is calling itself, not Container B.
hence localhost for A and B both are seperate.

💡 Then how do they communicate?
In this case:
The frontend can access the backend using:
http://backend:5000
Not:
❌ http://localhost:5000
Because:
backend is the service name, and Docker DNS will resolve it to the backend container's IP.

Example Use in Node.js Frontend
const response = await axios.post('http://backend:5000/submit', formData);

That works because:
Docker Compose puts both containers in the same network
backend is a valid hostname inside that network

✅ How to Make Services Talk to Each Other: use names that docker DNS resolves to(service discovery)
Use Docker Compose to run all services in the same custom network. Docker Compose does this by default.
docker-compose.yaml
version: '3.8'
services:
  frontend:
    image: yourname/frontend:latest
    environment:
      - BACKEND_URL=http://backend:5000/
    ports:
      - "3000:3000"
    depends_on:
      - backend
    networks:
      - app-network
  backend:
    image: yourname/backend:latest
    ports:
      - "5000:5000"
    networks:
      - app-network
  admin:
    image: yourname/admin:latest
    ports:
      - "4000:4000"
    networks:
      - app-network
networks:
  app-network: {}

A Dockerfile provides instructions to build a container image while a Compose file defines your running containers. 
Quite often, a Compose file references a Dockerfile to build an image to use for a particular service.

Why Use Docker Compose?
Because it lets you:
Define all services, ports, networks, volumes, etc. in one YAML file

Launch everything with one command:
>docker compose up

Automatically creates a shared network so containers can talk by service names

To shut down all containers at once that were started by docker compose:
>docker compose down
---

Old dev workflow 😒(need to bring down, then rebuild and make up and running if after any changes were made)
>docker compose down
>docker compose up --build
⏳ slow, annoying
or
volumes:
  - .:/app
❌ messy
❌ breaks node_modules / python venv
❌ permission issues


New dev workflow 😍(file sync+auto rebuild)
>docker compose watch

Docker:
Watches source files
Syncs only what changed
Restarts container if required
Rebuilds image if Dockerfile changes

Example: compose.yaml
services:
  web:
    build: .
    develop:
      watch:
        - action: sync
          path: ./src
          target: /app/src

        - action: rebuild
          path: package.json
Run:
docker compose watch
What happens:
| Change                | Action              |
| --------------------- | ------------------- |
| Edit `src/app.js`     | Sync into container |
| Change `package.json` | Rebuild image       |
| Change Dockerfile     | Rebuild             |
| No manual restart     | 🎉                  |

Actions supported in watch:
| Action         | Meaning                 |
| -------------- | ----------------------- |
| `sync`         | Copy files to container |
| `rebuild`      | Rebuild image           |
| `restart`      | Restart container       |
| `sync+restart` | Both                    |

✅ Use it when:
Developing Node / Python / Java apps
Want hot reload without bind mounts
Hate permission & performance issues
Using Docker Compose v2+

❌ Don’t use when:
Production
Simple one-off containers
Already using in-container watchers (nodemon, uvicorn)

=========================================================================================

Push docker images to dockerhub =>hub.docker.com(create an account on this)
1.Create dockerfiles/compose files

while pushing to repo ur image must be like : username/imagename

2.Build:
docker build -t <image_name> .            :'.'means current directory

3.Check:
docker images

4.Tag:
docker tag <image_name> <dockerhub_username>/<imagename>:<tag>

Tag and build in one command:
docker build -t <dockerhub_username>/<image>:<tag> .

5.Login
docker login    =>Enter username and password/access token of docker account

6.push
docker push <dockerhub_username>/<image>:<tag>
